# GitHub Research Wave 243 Backlog

Date: 2026-06-06

Theme: Spatial measuring, modeling, collaboration, and MR workbench surfaces.

## Completed In This Wave

- Studied `rtkCode/Sizer` as a browser AR measure/model/project donor with
  WebXR hit-test reticle, point-pair distance and angle capture, DOM overlay
  measurement cards, local history, A-Frame box editing, toolbar controls,
  drag/rotation gestures, AR.js marker projection, and marker-found/lost
  feedback.
- Studied `byte-banditt/Meshelanjelo` as an MR mesh manipulation donor with
  `OVRHand` pinch detection, pointer-pose deformation center, left-pull and
  right-push semantics, smoothed intensity/rebound, and a Burst/job
  `PushPullDeformer` operating on vertices/normals with radius falloff.
- Studied `B22DigitalTwins2022/ar-resilience-planner-v2` as an MR planning
  workbench with additive scene loading, persistent menu state, panel selector,
  solution grouping, simulation updates, and timestamped user-study CSV logs.
- Studied `adityanooka/Unity-Dive-VR` as a collaborative VR reference with
  Unity Netcode, XRI selection-count lift gates, server-owned random spawning,
  server-only proximity reactions, ownership-guarded movement, and VR/desktop
  locomotion fallback.
- Studied `Hempp/street-art-gallery` as a source-light product reference for
  social gallery surfaces: hotspots, guided tours, avatars, emotes, voice,
  nametags, gathering areas, and comfort settings.
- Added a reusable method entry for spatial workbench loops that capture,
  model, manipulate, project, synchronize, and log spatial state.

## Follow-Up Queue

1. Build a spatial-authoring matrix across Sizer, Meshelanjelo, prior CAD,
   drawing, and browser creative-surface waves.
2. Extract undo/history needs for AR measurement and hand-driven mesh editing.
3. Compare collaborative object gates across Unity Netcode, WebXR shared rooms,
   and Udon sync patterns.

## Do Not Spend Time On Yet

- Do not run browser AR, Unity, Meta hand tracking, or networking scenes.
- Do not treat source-light social gallery docs as implementation evidence.
- Do not copy old A-Frame/AR.js dependencies or course-project networking
  assumptions without modernization.
