# GitHub Research Wave 208 Backlog

Date: 2026-06-06

Theme: overlay media micro-surfaces and lower-bound overlay surface pipelines.

## Completed In This Wave

- Deepened `Yukiiro-Nite/notebook-vr-overlay` as a minimal note/image overlay bootstrap.
- Deepened `Daniel-Webster/WT-OpenVR-Overlay` as a Unity telemetry overlay shell with local service polling.
- Deepened `Wulkop/VolumeVR` as a CEF/windowless bootstrap reference, not a complete overlay donor.
- Deepened `iigomaru/MPVR` as a direct libmpv/OpenGL/OpenVR texture path prototype.
- Added a method-catalog entry for media/browser surface to OpenVR overlay texture loops.
- Added family-level synthesis for overlay media micro-surfaces.

## Follow-Up Queue

1. Compare `MPVR` against newer media-overlay approaches that separate playback engine, controls, and overlay compositor more cleanly.
2. Revisit `WT-OpenVR-Overlay` only if a future Unity overlay shell needs typed telemetry polling, dashboard thumbnails, or mouse-to-UI routing examples.
3. Treat `notebook-vr-overlay` as a lower-bound event-loop reference, not as a finished note-taking UX donor.
4. Treat `VolumeVR` as a browser runtime caveat until source shows actual overlay submission and input routing.
5. Build a future method matrix for surface producers: static image, native browser, Unity render texture, libmpv texture, desktop capture, and remote stream.

## Do Not Spend Time On Yet

- Do not attempt to run old binaries or bundled DLLs from `MPVR`.
- Do not treat these projects as shipping-quality overlay products.
- Do not promote `VolumeVR` into a strong donor without a later source pass that finds overlay submission code.
