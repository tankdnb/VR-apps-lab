# GitHub Research Wave 453 Backlog

- Wave: `453`
- Theme: `WebXR MRTK and Unity interaction bridge samples`
- Status: `completed`

## Completed

- Deduplicated against existing WebXR export and MRTK retrofit entries.
- Read WebXR template files, loader/settings assets, WebGL bridge files,
  hosted build layout, and README/device-support framing.
- Extracted WebXR exported interaction bridge method.
- Updated registry, families, methods, not-yet-studied queue, and indexes.

## Follow-up

- Build a small source-quality classification for WebXR repos:
  `source donor`, `bridge reference`, `generated build only`, and
  `product demo only`.
- Compare WebXR hand/controller input mapping against `webxr-samples`,
  `unity-webxr-export`, and `Simple-WebXR-Unity`.
- Track browser/runtime/device support labels with dates.

