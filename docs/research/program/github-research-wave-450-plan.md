# GitHub Research Wave 450 Plan

- Theme: haptic pattern libraries and device feedback routing.
- Shortlist:
  `bhaptics/haptic-guide`, `bhaptics/haptic-library`.
- Dedupe result:
  `haptic-guide` was new; `haptic-library` was a partially studied node
  deepened for playback API and Unity/C# samples.
- Read-only rule:
  source and docs were inspected only; no project was run, built, installed, or
  launched.
- Required outputs:
  wave doc, registry, families, method, backlog, and indexes.
