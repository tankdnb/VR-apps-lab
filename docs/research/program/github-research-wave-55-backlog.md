# GitHub Research Wave 55 Backlog

- Date: `2026-04-19`
- Scope: next GitHub discovery wave focused on
  `creator control overlays`, `research stations`, and
  `specialized companion-presence surfaces`.

## Status legend

- `Done`
- `Next`

## Work package A: Search and shortlist

- `Done` Search GitHub for OBS control overlays, questionnaire overlays, companion-visualization overlays, and anchored-awareness overlays
- `Done` Deduplicate the results against the registry
- `Done` Freeze a bounded shortlist that spans creator control, VR study tooling, remote buddy visualization, and anchored object awareness

## Work package B: Local source acquisition

- `Done` Confirm `VRBro-Overlay` in local cache
- `Done` Confirm `ROVER` in local cache
- `Done` Confirm `steamvr-overlay-vrbuddy` in local cache
- `Done` Confirm `VRPoleOverlay` in local cache
- `Done` Verify that local source cache remains outside git tracking

## Work package C: Code-level deep pass

- `Done` Inspect OBS plugin networking, wrist menu, and dashboard overlay split in `VRBro-Overlay`
- `Done` Inspect overlay manager, desktop UI, study import, and study sequencing in `ROVER`
- `Done` Inspect remote tracking ingress and multi-overlay body-part rendering in `steamvr-overlay-vrbuddy`
- `Done` Inspect config model, chaperone-aware styling, and controller-drag calibration in `VRPoleOverlay`

## Work package D: Repository updates

- `Done` Add Wave 55 plan document
- `Done` Add Wave 55 backlog document
- `Done` Add Wave 55 synthesis document
- `Done` Update the project registry for creator, research, and companion overlay surfaces
- `Done` Update relevant overlap families
- `Done` Update `not-yet-studied-deeply.md` with honest follow-up nodes where needed
- `Done` Update the methods catalog with creator-control, research-station, and companion-visualization methods
- `Done` Update documentation indexes to include Wave 55

## Work package E: Verification and publish

- `Done` Verify local source cache is still ignored
- `Done` Review git status and documentation integrity
- `Done` Verify the new wave is linked from the documentation indexes
- `Next` Commit the wave results
- `Next` Push the updated research base to GitHub

## Follow-up candidates after this wave

- `Next` Keep `VRBro-Overlay` visible whenever a future branch needs `creator control surface with local plugin protocol` rather than a general desktop panel
- `Next` Keep `ROVER` visible whenever a future branch needs a `standalone VR study station` rather than an overlay host plus ad hoc survey pages
- `Next` Revisit `steamvr-overlay-vrbuddy` only if a future branch needs a deeper look at `shared-space companion visualization` and richer identity or smoothing layers
