# GitHub Research Wave 49 Backlog

- Date: `2026-04-19`
- Scope: next GitHub discovery wave focused on
  `OSCQuery companion frameworks`, `AI and automation bridges`, and
  `avatar-parameter smoothing or refinement layers`.

## Status legend

- `Done`
- `Next`

## Work package A: Search and shortlist

- `Done` Search GitHub for avatar-facing companion shells, `OSCQuery` libraries, automation relays, and OSC smoothing helpers
- `Done` Deduplicate the results against the registry
- `Done` Freeze a bounded shortlist that spans diagnostics-rich desktop hosts, reusable discovery libraries, AI or automation relays, editor-side smoothing helpers, and one weaker mixed-helper comparison node

## Work package B: Local source acquisition

- `Done` Confirm `OscGoesBrrr` in local cache
- `Done` Confirm `VRCOSCUtils` in local cache
- `Done` Confirm `vrc-oscquery-lib` in local cache
- `Done` Confirm `vrchat-mcp-osc` in local cache
- `Done` Confirm `OSCmooth` in local cache
- `Done` Verify that local source cache remains outside git tracking

## Work package C: Code-level deep pass

- `Done` Inspect typed IPC, diagnostics surfaces, and service boundaries in `OscGoesBrrr`
- `Done` Inspect feature split and monolithic helper shape in `VRCOSCUtils`
- `Done` Inspect discovery builders, examples, and service model in `vrc-oscquery-lib`
- `Done` Inspect relay server, shared types, and MCP-facing tools in `vrchat-mcp-osc`
- `Done` Inspect editor generation flow and animator-side smoothing layers in `OSCmooth`

## Work package D: Repository updates

- `Done` Add Wave 49 plan document
- `Done` Add Wave 49 backlog document
- `Done` Add Wave 49 synthesis document
- `Done` Update the project registry for companion frameworks, automation relays, and smoothing layers
- `Done` Update overlap families for broader avatar-facing OSC companion tooling
- `Done` Update `not-yet-studied-deeply.md` with honest follow-up nodes
- `Done` Update the methods catalog with `OSCQuery`, relay, and smoothing methods
- `Done` Update documentation indexes to include Wave 49

## Work package E: Verification and publish

- `Done` Verify local source cache is still ignored
- `Done` Review git status and documentation integrity
- `Done` Verify the new wave is linked from the documentation indexes
- `Next` Commit the wave results
- `Next` Push the updated research base to GitHub

## Follow-up candidates after this wave

- `Next` Compare `OscGoesBrrr`, `vrchat-mcp-osc`, and `vrc-osc-manager` directly as `desktop companion`, `AI relay`, and `plugin host`
- `Next` Keep `vrc-oscquery-lib` visible whenever a future branch needs `discovery and auto-connect` instead of hardcoded ports
- `Next` Revisit `VRCOSCUtils` only if a later pass needs a broader comparison of weakly-structured `mixed helper` repos
