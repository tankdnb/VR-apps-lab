# GitHub Research Wave 447 Plan

- Theme: Unity XRI interaction learning and affordance baselines.
- Goal: treat a large learning repository as an interaction primitive map rather
  than an import candidate.
- Shortlist:
  `mattdway/CreateWithVR`.
- Dedupe result:
  `mattdway/CreateWithVR` was new; `Unity-Technologies/XR-Interaction-Toolkit-Examples`
  was considered but not re-synced during this pass, so it was not claimed as
  newly studied.
- Read-only rule:
  source and documentation were inspected only; no project was run, built,
  installed, or launched.
- Required outputs:
  wave landscape doc, registry entry, family entry, method update, and follow-up
  backlog.
