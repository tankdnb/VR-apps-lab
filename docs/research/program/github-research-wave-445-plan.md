# GitHub Research Wave 445 Plan

- Theme: XR radial hand menu controls and marking-menu primitives.
- Goal: extract reusable menu activation, angular selection, hand-attached
  launcher, submenu, hover/commit, and fallback input patterns.
- Shortlist:
  `connorpugh/XRRadialMarkingMenu`,
  `jesuisse/godot-radial-menu-control`,
  `Oyshoboy/RadialMenuVR`,
  `NovaUI-Unity/XRHandMenuSample`.
- Dedupe result:
  `connorpugh` and `jesuisse` were new; `Oyshoboy` and `NovaUI-Unity` were
  deepened as overlap/reference nodes.
- Read-only rule:
  source and documentation were inspected only; no project was run, built,
  installed, or launched.
- Required outputs:
  wave landscape doc, registry entry, family entry, method update, and follow-up
  backlog.
