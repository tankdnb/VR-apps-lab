# GitHub Research Wave 333 Backlog - WebXR Capability Probes, Pass-Through Testbeds, and Lightweight Utility Labs

## Executed Scope

- Searched and deduplicated WebXR utility/testbed projects.
- Froze a three-project shortlist because the theme is narrow and several
  broader WebXR UI/input candidates were already studied.
- Read source and documentation statically from local-only cache.
- Extracted browser capability matrices, spec-linked feature rows, exportable
  JSON reports, WebRTC/media/WebXR/WebGPU checks, pass-through WebGL plane
  rendering, AR hit-test placement helpers, boundary-box helpers, and thin lab
  index caveats.

## Studied Projects

- `rwth-acis/i5-Toolkit-for-WebXR`
- `gareth-morgan-nv/WebXR-utils`
- `webvrdev/webvrdev-labs`

## Backlog Findings

- Treat capability checks as a browser-side XR doctor pattern.
- Treat pass-through/video-plane code as an experiment reference, not a
  production overlay stack.
- Keep `webvrdev-labs` as a weak product-reference placeholder until real
  tools are published under that lab.

## Completion Criteria

- Wave landscape document exists.
- Registry and families include all studied projects.
- Method catalog captures browser XR capability and pass-through testbed
  boundaries.
- Follow-up gaps are queued.
- Local-only cache is cleaned before commit.
