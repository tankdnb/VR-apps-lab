# GitHub Research Wave 448 Plan

- Theme: SteamVR dashboard overlay control and domain panel surfaces.
- Shortlist:
  `haolink/VRCOSCAvatarScaleOverlay`, `Mon-Ouie/mpris-openvr-overlay`.
- Dedupe result:
  `haolink` was new; `Mon-Ouie` was an existing node used for source-level
  deepening and comparison.
- Read-only rule:
  source and docs were inspected only; no project was run, built, installed, or
  launched.
- Required outputs:
  wave doc, registry, families, method, backlog, and indexes.
